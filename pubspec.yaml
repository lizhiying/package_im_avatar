name: im_avatar
description: A simple to use avatar widget, with support for selecting a new avatar. The avatar can be selected from camera or gallery.
version: 0.2.0
homepage: https://twitter.com/imujtaba8488
repository: https://github.com/imujtaba8488/package_im_avatar.git
issue_tracker: https://github.com/imujtaba8488/package_im_avatar/issues

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  permission_handler: ^10.0.0
  image_picker: ^0.8.5+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/

